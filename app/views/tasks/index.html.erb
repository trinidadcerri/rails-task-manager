<h1>My Tasks</h1>
<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, tasks_path(task) %></li>
    <%# <li><%= link_to task.delete, tasks_path(task) data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} </li>%>
  <% end %>
</ul>
